{
  "name": "glt-admin-worker",
  "version": "1.0.0",
  "description": "A Telegram Bot Cloudflare Worker",
  "main": "src/worker.js",
  "scripts": {
    "build": "wrangler types && tsc",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "typescript": "^5.8.3",
    "wrangler": "^4.10.0"
  },
  "dependencies": {
    "@types/node": "^22.14.1",
    "@types/node-telegram-bot-api": "^0.64.8"
  }
}
